# Base folder for Designer Folders. 
# The folder picker defaults to here, 
# and the user chooses one of its subfolders as the project folder
DESIGNER_CONFIG_FOLDER=/base/path/for/design/files

# Path to Google Cloud Vision credentials. These are stored as a json file
GOOGLE_APPLICATION_CREDENTIALS=/path/to/json/file/

# Gemini vision OCR API key (for LLM-based OCR in FieldIndexer)
# Obtain a key from https://aistudio.google.com/apikey
GOOGLE_API_KEY=your_gemini_api_key_here
# or
# GEMINI_API_KEY=your_gemini_api_key_here


# DEPRECATED
LOGO_PATH=path/to/your/logo.png

# DEPRECATED
MULTIPAGE_TIFF=path/to/your/form.tif

# DEPRECATED
JSON_FOLDER = directory/for/json/page/descriptors

# Import folder and validations are configured per project in json/project_config.json:
# {
#   "batch_folder": "c:\\My Project",
#   "import_filename": "EXPORT.TXT",
#   "always_review": ["Field3", "Another field"],
#   "lookup_list": "optional/path/to/lookup.csv",
#   "pages_without_fiducial": [0],
#   "validations": [
#     {"strategy": "max_tickboxes", "field_names": ["q1_a", "q1_b"], "params": {"max": 1}},
#     {"strategy": "value_exists_in_lookup", "field_names": ["herd_number"], "params": {"lookup_column": 0}}
#   ]
# }