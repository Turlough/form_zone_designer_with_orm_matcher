# Form Zone Designer
This is a PyQt6 app that allows the user to load a scanned multipage form (or survey, etc) 
as a template. Multipage tiff is the expected format.

Thumbnails of each page are shown on the left. 
When the user clicks a thumbnail, the page is displayed on the right hand panel